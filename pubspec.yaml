name: local_player
description: Private offline-first media client (personal / educational use).
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  # Phase 2+ (uncomment when wiring real data/audio):
  # audioplayers: ^6.1.0
  # path_provider: ^2.1.4
  # http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
